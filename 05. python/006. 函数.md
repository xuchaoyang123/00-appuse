## 六、函数


### 1. 创建
```python

#函数
#类似oracle的存储过程

#创建
def my_len():
    i=0
    for k in s:
        i+=1
    print(i)
    return i  #返回值


#执行
s='12345'
my_len()
>>>5

```

### 2. 返回值
```python

#没有返回值  默认返回Noce
    #不写return 就没有返回值
    #只写return,代表结束整个函数
    #return None  写法就是直接返回None ---不常用

#返回1个值
#可以返回任何数据类型
#只要return返回了,就可以接收到
#如果在一个程序中有多个return那么只执行第一个

#返回多个值
#多个返回值用多个变量接收,有多少返回值就用多少变量接收,不能少。
#返回多个值用一个变量接收,得到的是一个元组

元组是可以解包的
a,b,c=(1,2,3)
a  #1
b  #2
c  #3

```

### 3. 参数
```python

#没有参数
#定义函数和调用函数时括号里都不写内容

#有一个参数
#传什么就是什么

#有多个参数


#站在实际参数的角度上 ---> my_len('12345678') 
按照位置传参
按照关键字传参
             混着用可以,但是必须先按照位置传参在按照关键字传参
             不能给同一个变量传多个值 

#站在位置参数的角度上 ---->  def my_len(a,b):
               位置参数:  必须传的参数,有几个参数就传几个值
               默认参数：可以不传,如果不传就是用默认的参数,如果传了就用传的
       例:  def my_len(name,sex='男'):


#只有调用函数的时候   ---> my_len('12345678') 
   按照位置传:  直接写参数的值
   按照关键字：关键字=值

#定义函数的时候     ---->  def my_len(a,b):
               位置参数：直接定义参数
    默认参数：关键字参数：参数名='默认的值'
    动态参数：可以接收任意多个参数
参数名之前加* 习惯参数名args
参数名之前加** 习惯参数名kwargs
args必须在kwargs之前

    顺序：位置参数,*args,默认参数,**kwargs

```

### 4. 动态参数args/kwargs


```python


 #动态参数有俩种
    *args     接收的按照位置传参的值,组织成一个元祖
例：def sum(*args):
    print(args)
sum(1,2,3,4)
sum(1,2)

  **kwargs 接收的是按照关键字传参的值,组织成一个字典
    例:    def sum(**kwargs): 
    print(kwargs)
sum(a=1,b=2,c=3)
sum(a=1,b=2)
   
  
#动态参数的另一种传参方式

def func(*args): #站在形参的角度上给变量加上*
就是组合所有传来的值
    print(args)

func(1,2,3,4,5)
l=[1,2,3,4,5]  
#实现成这样假如有很多个怎么写
#func(l[0],l[1],l[2],l[3])
func(*l)	#站在实参的角度上给一个序列加上*
  就是将这个顺序打散。


def func(**kwargs):
    		print(kwargs)

func(a=1,b=2)
d={'a':1,'b':2}
func(**d)

```


### 5. 函数的注释

```python


def func():
    '''
    这个函数实现了什么功能
    参数1：
    参数2：
    return: 是字符串或者列表的长度
    '''

```

### 5. 默认参数的陷阱

```python
如果默认参数的值是一个可变的数据类型,
那么每一次调用函数的时候,
如果不传值就公用这个数据类型的资源

def qqxing(l=[]):
    l.append(1)
    print(l)
qqxing()    #[1]
qqxing([])  #[1]  
qqxing()    #[1,1]


def qqxing(k,l={}):
    l[k]='v'
    print(l)
qqxing(1)    
qqxing(2)    #第二次调用公用的第一次的字典内容所以有1:v
qqxing(3)    #同理从始至终操作的是一个字典
  {1: 'v'}  
		{1: 'v', 2: 'v'}
		{1: 'v', 2: 'v', 3: 'v'}

```

### 6. 传递参数

```python

#定义
def my_len(s): #自定义函数只需要0个参数,接收参数，形式参数=形参
    i =0
    for k in s:
        i+=1
    print(k)
    return i #返回值

#执行
my_len('12345678') #传递参数=传参,实际参数=实参

函数里面调用函数

```



### 7. 函数的一些功能
```python

#函数的一些功能

def wahah ():
    '''
    aaaaaaaa
    :return:
    '''
    print('哇哈哈')
print (wahah.__name__)   #查看字符串格式的函数名
print (wahah.__doc__)   #查看这个函数的注释


```



